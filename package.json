{
  "name": "rage-tpc-server",
  "license": "ISC",
  "scripts": {
    "build": "pnpm run -r build",
    "clean": "pnpm run -r clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^4.4.0",
    "typescript": "^5.0.2"
  }
}
